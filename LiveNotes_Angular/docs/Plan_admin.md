Quiero agrupar todas las herramientas de debug (el boton de idiomas, activar / desactivar el premium) junto a una vista que sea una lista de tickets (Esta debe permitir gestionarlos como eliminarlos y tal) para que solo sean vistas por un usuario admin, Añadido a esto, quiero un toggle entre usuario admin y cliente para que el administrador pueda ver cómo lo ven los clientes. Dame el plan de esto